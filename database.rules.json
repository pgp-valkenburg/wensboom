{
  "rules": {
    ".read": "auth.token.system === 'wishes'",
    ".write": "auth.token.system === 'wishes'",
    "wishes": {
      "$wish": {
        "body": { ".validate": true },
        "approved": { ".validate": true },
        "$other": { ".validate": false }
      }
    }
  }
}
